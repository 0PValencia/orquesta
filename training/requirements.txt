# Dependencias para fine-tuning (GPU NVIDIA + CUDA recomendado)
# Instalar en un venv dedicado.

# Core (versiones flexibles; Unsloth puede fijar pins al instalar)
torch
transformers
datasets
accelerate
peft
trl
bitsandbytes

# Opción recomendada para entrenar rápido en una sola GPU:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# o seguir https://github.com/unslothai/unsloth#installation

# Utilidades
pyyaml
sentencepiece
protobuf
